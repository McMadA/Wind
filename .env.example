# ── OneDrive / Microsoft Graph ──
ONEDRIVE_CLIENT_ID=your-azure-app-client-id
ONEDRIVE_CLIENT_SECRET=your-azure-app-client-secret
ONEDRIVE_TENANT_ID=common
ONEDRIVE_REDIRECT_URI=http://localhost:8400

# ── Google Drive ──
# Path to the OAuth credentials JSON downloaded from Google Cloud Console
GOOGLE_CREDENTIALS_FILE=credentials.json

# ── iCloud ──
APPLE_ID=your_apple_id@example.com
APPLE_PASSWORD=your_apple_password

# ── Sync settings ──
# Source and Destination services: onedrive, gdrive, icloud, or gphotos
SOURCE_SERVICE=onedrive
DEST_SERVICE=gdrive
# Source and Destination paths (folder path / for root, or folder ID)
SOURCE_PATH=/
DEST_PATH=root
# Local temp directory for intermediate file storage
TEMP_DIR=.sync_temp
